version: '3.8'

services:
  jenkins:
    build: .
    container_name: jenkins-maven-java
    ports:
      - "8080:8080"
    volumes:
      - jenkins_data:/var/jenkins_home
      - .:/var/jenkins_home/workspace/my-project
    restart: always

volumes:
  jenkins_data: